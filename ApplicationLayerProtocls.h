#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

virtual class ApplicationLayerProtocols {

    public:
        const static int HTTP   = 0;
        const static int FTP    = 1;
        const static int SMTP   = 2;
        const static int DNS    = 3;
        const static int TELNET = 4;
        const static int IM     = 5;
        const static int RIP    = 6;
        const static int NFS    = 7;
    
}
